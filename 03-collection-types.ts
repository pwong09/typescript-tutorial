// Arrays
let list: number[] = [1, 2, 3];

let anotherList: Array<number> = [4, 5, 6];

// Tuples - can hold mixed types
// variableName: [type, type, ...]

let person1: [string, number] = ['Marcia', 35];
// below will throw an error
// let person2: [string, number] = ["Peter", 25, true];

// order of values must match order of types
